import {StyleSheet} from 'react-native';
import variables, {width_screen} from '../../../utils/utils';

const styles = StyleSheet.create({
  main: {flex: 1, backgroundColor: variables.colorWhite, width: width_screen},
});
export default styles;
